<div class="top">
  <h1>ユーザ一覧</h1>
</div>

<div class="content">
  <h1>運営<h1>
  <div>
    <% @users.where(grade: 0).each do |user| %>
      <div>
        <%= link_to(user.name, "/users/#{user.id}") %>
      </div>
    <% end %>
  </div>
</div>

<div class="content">
  <h1>１期生<h1>
  <div>
    <% @users.where(grade: 0).each do |user| %>
      <div>
        <%= link_to(user.name, "/users/#{user.id}") %>
      </div>
    <% end %>
  </div>
</div>

<div class="content">
  <h1>２期生<h1>
  <div>
    <% @users.where(grade: 0).each do |user| %>
      <div>
        <%= link_to(user.name, "/users/#{user.id}") %>
      </div>
    <% end %>
  </div>
</div>

<div class="content">
  <h1>３期生<h1>
  <div>
    <% @users.where(grade: 0).each do |user| %>
      <div>
        <%= link_to(user.name, "/users/#{user.id}") %>
      </div>
    <% end %>
  </div>
</div>

<div class="content">
  <h1>４期生<h1>
  <div>
    <% @users.where(grade: 0).each do |user| %>
      <div>
        <%= link_to(user.name, "/users/#{user.id}") %>
      </div>
    <% end %>
  </div>
</div>