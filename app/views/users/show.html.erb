<h2>ユーザー</h2>

<div class="main user-show">
  <div class="container">
    <div class="user">
      <% @user.posts.each do |post| %>
        <%= link_to(post.content, "/users/#{post.id}") %>
      <% end %>
    </div>
  </div>
</div>